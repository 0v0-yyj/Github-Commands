:point_right: __download signle file from Git__
```github
curl -o filename.format "URL" %URL should raw file path in git
```
```github
wget -o filename.format "URL" %URL should raw file path in git
```
